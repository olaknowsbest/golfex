# Claude Code Prompt 08 — Login Holding Page

Copy the block below into Claude Code in `/Users/mac/Desktop/golf/site`.

```text
Build Prompt 08: a polished public Login holding page for the fictional Nazario Kayla prototype.

Read `CLAUDE.md`, the core docs, completed prompts/pages, `login.html`, and shared shell CSS. Use the NK Management login/dashboard references only for premium dark-panel hierarchy, spacing, and clear status treatment. Do not copy its form, identity, dashboard, user data, functionality, or blue styling.

Preserve branch `athlete-platform-concept`, dirty worktree, shared shell, NK loader, and all completed pages; do not commit. Scope is only `login.html` plus one new `assets/css/page-login-holding.css`.

Replace the placeholder body with a concise holding page:

1. MAIN ACCESS PANEL
   - Eyebrow: `ACCOUNT ACCESS · FUTURE RELEASE`
   - H1: `Secure account access is coming later.`
   - Explain that login is intentionally unavailable until authentication, backend architecture, privacy controls, and account isolation are approved and tested.
   - Visible badge: `Prototype only · Login unavailable`
   - Show the existing NK monogram as a brand element, not as a fake avatar.

2. SECURITY COMMITMENTS
   - H2: `Account access will open only when it is ready.`
   - Three concise, noninteractive cards:
     - `Protected identity` — verified authentication and secure recovery.
     - `Private account data` — customer records isolated by authorization rules.
     - `Auditable access` — important account/admin actions recorded appropriately.
   - These are future requirements, not claims that functionality exists now.

3. PUBLIC-SITE LINKS
   - H2: `Explore the public prototype.`
   - `Return home` → `index.html`
   - `View Fan Cards` → `pricing-plan.html`
   - `Contact overview` → `contact-us.html`
   - Note: `No account, password, session, or personal information is created or stored in this build.`

Do not add a login/registration form, email or password fields, social login, forgot-password link, account/dashboard preview, workflow steps, demo credentials, disabled fake submit button, validation, cookies, local/session storage, authentication library, API, backend simulation, or success/error state.

Update title/meta description and remove visible Golfex placeholder copy. Keep one H1, logical headings, accessible links/focus, and the established cream/black/sand/yellow system. Make it premium but intentionally compact; avoid a long page. Translate only the NK Management reference’s visual polish into this brand.

Verify locally near 1280px, 768px, and actual ~390px where possible: no overflow, active Login nav, NK loader, mobile menu, footer, skip link, all three local links, console, one H1, and zero form/input/auth/storage code. Report exact files and deferrals; do not commit.

Before editing, state the Login-holding-page-only scope and expected files. Then implement and verify.
```
